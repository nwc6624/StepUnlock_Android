package com.stepunlock.app

import android.app.Application

class StepUnlockApplication : Application() {
    
    override fun onCreate() {
        super.onCreate()
        
        // TODO: Initialize default data when we add back the data layer
    }
}
